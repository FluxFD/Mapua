import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      mode: 'light',
      primary: {
        main: '#1B365B',
      },
      secondary: {
        main: '#D63A3F',
      },
    },
  });

export default theme;